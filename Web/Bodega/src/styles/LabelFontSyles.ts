import { makeStyles, typographyStyles } from "@fluentui/react-components";

export const useStyles = makeStyles({
  textCabecera: typographyStyles.subtitle2,
  textBody: typographyStyles.caption1,
});
